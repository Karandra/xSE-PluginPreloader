[General]
;if you have mod that uses DLL used by preloader
;you can rename DLL from your mod and write its new name here.
;Empty by default
;
;For example preloader usues 'IpHlpAPI.dll' and your mod uses it.
;Rename mod's DLL to something like 'IpHlpAPI MyMod.dll' and chnage below value to:
;OriginalLibrary=IpHlpAPI MyMod.dll
OriginalLibrary=

;Load method for xSE plugins.
;Delayed by default.
;
;Direct			0
;Delayed		1
;
;Delayed for MO, Direct for anything else.
LoadMethod=1

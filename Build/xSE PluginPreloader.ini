[General]
;if you have mod that uses DLL used by preloader
;you can rename DLL from your mod and write its new name here.
;Empty by default
;
;For example preloader usues 'X3DAudio1_7.dll' and your mod uses it.
;Rename mod's DLL to something like 'X3DAudio1_7 MyMod.dll' and chnage below value to:
;OriginalLibrary=X3DAudio1_7 MyMod.dll
OriginalLibrary=

;Load method of xSE plugins.
;Delayed by default.
;
;Direct			0
;Delayed		1
;
;Delayed for MO, Direct for anything else.
LoadMethod=1
